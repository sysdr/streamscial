[tool:pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = 
    -v
    --tb=short
    --asyncio-mode=auto
    --cov=src
    --cov-report=term-missing
asyncio_mode = auto
